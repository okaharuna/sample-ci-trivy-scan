# AWS SDK for Python
boto3>=1.26.0,<2.0.0

# HTTP requests library
requests>=2.31.0,<3.0.0

# JSON web token
PyJWT>=2.8.0,<3.0.0

# Flask (intentionally using older version with known vulnerabilities for testing)
Flask==2.2.0

# Jinja2 (older version with known vulnerabilities)
Jinja2==3.0.0
